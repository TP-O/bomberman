package components.ui.image;

import components.ui.Element;

public abstract class Image extends Element {

    public Image(int column, int row, int left, int right, int top, int bottom) {
        super(column, row, left, right, top, bottom);
    }
}
