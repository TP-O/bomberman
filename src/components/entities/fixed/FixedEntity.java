package components.entities.fixed;

import components.entities.Entity;
import core.Handler;

public abstract class FixedEntity extends Entity
{
    public FixedEntity(Handler handler)
    {
        super(handler);
    }
}
